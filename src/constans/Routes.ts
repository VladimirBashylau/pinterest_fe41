export enum Routes {
  Home = "/",
  // AddPost = "/add_post",
  Register = "/register",
  // Login = "/login",
  // Verify = "/verify/:uid/:token",
  Post = "/post/:id",
  PostItem = "/postitem/:id",
}
